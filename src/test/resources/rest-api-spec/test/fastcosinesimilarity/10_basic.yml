# Integration tests for the expert scoring script example plugin
#
"Plugin loaded":
#  6.5+
#- skip:
#    reason: "contains is a newly added assertion"
#    features: contains
- do:
    cluster.state: {}

# Get master node id
- set: { master_node: master }

- do:
    nodes.info: {}

# 6.5+
#- contains:  { nodes.$master.plugins: { name: fast-cosine-similarity } }
- match: { nodes.$master.plugins.0.name: fast-cosine-similarity  }
